<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PercussionApp"
        tools:targetApi="31">
        <activity
            android:name=".AnalyseActivity"
            android:exported="false"
            android:label="@string/title_activity_analyse"
            android:theme="@style/Theme.PercussionApp" />
        <activity
            android:name=".ListenActivity"
            android:exported="false"
            android:label="@string/title_activity_listen"
            android:theme="@style/Theme.PercussionApp" />
        <activity
            android:name=".PracticeActivity"
            android:exported="true"
            android:label="@string/title_activity_practice"
            android:theme="@style/Theme.PercussionApp"></activity>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="PercussionPro"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.PercussionApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>